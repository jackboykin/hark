[pytest]
testpaths = scenarios harness
pythonpath = .
python_files = test_*.py
markers =
    network: requires real internet (excluded by default)
    differential: requires Unbound binary (excluded by default)
    slow: takes >5s
addopts = -ra --tb=short -m "not network and not differential"
